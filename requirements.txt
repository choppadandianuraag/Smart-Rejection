# Smart Rejection - Section-Aware Resume Extraction System (V2)
# Core Dependencies

# Supabase for database (with pgvector support)
supabase>=2.0.0

# PDF Processing
PyPDF2>=3.0.0
pdfplumber>=0.10.0
pdf2image>=1.16.0

# Document Processing (DOCX, DOC)
python-docx>=1.0.0
mammoth>=1.6.0

# Image Processing & OCR
Pillow>=10.0.0
pytesseract>=0.3.10

# NuMarkdown-8B-Thinking (optional, via Hugging Face Transformers)
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.27.0
bitsandbytes>=0.42.0

# BERT Embeddings (NEW for V2)
sentence-transformers>=2.2.2

# Text Processing
python-magic>=0.4.27
chardet>=5.2.0

# Scientific Computing (for embeddings)
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Environment & Config
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
email-validator>=2.0.0

# Utilities
tqdm>=4.66.0
loguru>=0.7.0

# API Framework (for webhook server)
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
httpx>=0.27.0

# LangChain & Groq (for existing ATS ranking system - keep for backward compatibility)
langchain-groq>=0.0.1
langchain-core>=0.1.0
